{
  "_comment": "Snippet for ~/.claude.json — merge this mcpServers entry into your existing file. Run `npx @qmediat.io/gemini-code-context-mcp init` first to create the credentials profile securely.",
  "mcpServers": {
    "gemini-code-context": {
      "command": "npx",
      "args": ["-y", "@qmediat.io/gemini-code-context-mcp"],
      "env": {
        "GEMINI_CREDENTIALS_PROFILE": "default"
      }
    }
  }
}
